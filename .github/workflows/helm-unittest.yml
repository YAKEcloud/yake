name: Run helm unittests

on: pull_reques

jobs:
  unittest:
    runs-on: 23ke-runner
    steps:
      - uses: actions/checkout@v3
      - uses: d3adb5/helm-unittest-action@v2
        with:
          github-token: ${{ secrets.GH_TOKEN_23T_MACHINE_USER }}
      - run: ./hack/run-helm-unittest.sh
