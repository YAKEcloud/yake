extends: default

rules:
  comments: disable
  line-length: disable
  truthy: disable
  document-start: disable

ignore: |
  /helmcharts/
  kustomization.*
  /hack/helmchart-import/secret-kubeconfig-for-gardener.yaml
  /hack/secrets/apiserver-in-shoot-kubeconfig.yaml
  /hack/secrets/shoot-kubeconfig.yaml
