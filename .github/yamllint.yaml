extends: default

rules:
  comments: disable
  line-length: disable
  truthy: disable
  document-start: disable

ignore: |
  /helmcharts/
  kustomization.*
  /hack/ci/secrets/*
  /hack/ci/misc/microservices-demo.yaml
