extends: default

rules:
  comments: disable
  line-length: disable
  truthy: disable
  document-start: disable

ignore: |
  /helmcharts/
  kustomization.*
  /hack/helmchart-import/secret-kubeconfig-for-gardener.yaml
  /hack/ci/secrets/apiserver-in-shoot-kubeconfig.yaml
  /hack/ci/secrets/shoot-kubeconfig.yaml
