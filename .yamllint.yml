extends: default

rules:
  truthy:
    check-keys: false
  document-start: disable

ignore: |
  /hack/ci/secrets/*
  /hack/ci/misc/microservices-demo.yaml
  base-install/helmcharts/*
  base-addons/vertical-pod-autoscaler/*
