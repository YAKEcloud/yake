apiVersion: v1
kind: Secret
metadata:
  name: extensions-values
  namespace: flux-system
type: Opaque
stringData:
  values.yaml: |
    os-ubuntu:
      enabled: true
    os-gardenlinux:
      enabled: true
    networking-calico:
      enabled: true
    networking-cilium:
      enabled: true
    provider-azure:
      enabled: true
