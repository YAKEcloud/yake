apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: addons
  namespace: flux-system
spec:
  interval: 1m
  dependsOn:
    - name: configuration
  sourceRef:
    kind: GitRepository
    name: 23ke
  path: ./addons
  prune: true
