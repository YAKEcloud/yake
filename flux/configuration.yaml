apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: configuration
  namespace: flux-system
spec:
  interval: 1m
  dependsOn:
    - name: 23ke-env-config
  sourceRef:
    kind: GitRepository
    name: 23ke
  path: ./configuration
  prune: true
