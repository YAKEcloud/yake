apiVersion: v1
kind: ConfigMap
metadata:
  name: dashboard-base-values
  namespace: flux-system
data:
  values.yaml: |-
    apiServerUrl: https://api.{{ .Values.baseDomain }}/
    sessionSecret: {{ .Values.dashboardSessionSecret }}
    ingress:
      hosts:
      - dashboard.{{ .Values.baseDomain }}
      annotations:
        kubernetes.io/ingress.class: nginx
        cert-manager.io/cluster-issuer: default-issuer
        dns.gardener.cloud/dnsnames: "*"
        dns.gardener.cloud/ttl: "600"
        dns.gardener.cloud/class: base-dns-class
    oidc:
      issuerUrl: https://identity.{{ .Values.baseDomain }}/oidc
      # clientId is the public identifier of the gardener-dashboard aplication
      clientId: dashboard
      # clientSecret is the private secret of the gardener-dashboard aplication
      clientSecret: {{ .Values.dashboardClientSecret }}
      # redirectUri is the endpoint receiving the OTAC from the OpenID provider
      # after successful login
      redirectUri: https://dashboard.{{ .Values.baseDomain }}/auth/callback
