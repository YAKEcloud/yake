apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-all-garden-kube-apiserver
  namespace: garden
spec:
  podSelector:
    matchLabels:
      component: kube-apiserver
  ingress:
    - {}
  egress:
    - {}
  policyTypes:
  - Ingress
  - Egress
