apiVersion: v1
kind: Secret
metadata:
  name: cert-management-values
  namespace: flux-system
type: Opaque
stringData:
  values.yaml: |
    createCRDs:
      issuers: false
      certificates: false
    configuration:
      defaultIssuerDomainRanges: internal.${AZURE_DOMAIN}
