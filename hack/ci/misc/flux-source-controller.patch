spec:
  template:
    spec:
      containers:
      - name: manager
        volumeMounts:
        - mountPath: /etc/ssl/certs/le-staging.pem
          name: le-staging
          subPath: le-staging.pem
      volumes:
      - name: le-staging
        configMap:
          name: le-staging
