apiVersion: v1
kind: Secret
metadata:
  name: extensions-values
  namespace: flux-system
type: Opaque
stringData:
  values.yaml: |
    os:
      ubuntu:
        enabled: true
    provider:
      hcloud:
        enabled: true
        values:
          test:
            test: hi
          image:
            nap: hallo

    networking:
      calico:
        enabled: true

    shoot:
      certService:
        enabled: true
      dnsService:
        enabled: true

    dnsManagement:
      enabled: true
