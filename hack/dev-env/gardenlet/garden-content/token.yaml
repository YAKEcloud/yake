apiVersion: v1
kind: Secret
metadata:
  name: bootstrap-token-5cd0b8 # last digits must be equal to token-id
  namespace: kube-system
type: bootstrap.kubernetes.io/token
stringData:
  token-id: "5cd0b8" # must be 6 characters
  token-secret: "56269d70ea98d215" # 16 characters
  usage-bootstrap-authentication: "true"
  usage-bootstrap-signing: "true"
