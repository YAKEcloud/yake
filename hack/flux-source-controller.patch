spec:
  template:
    spec:
      containers:
      - name: manager
        volumeMounts:
        - mountPath: /etc/ssl/certs
          name: le-staging
      volumes:
      - configMap:
          name: le-staging
        name: le-staging
