{{- range .Values.global.additionalSecrets }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .name }}
  namespace: {{ .namespace }}
stringData: 
  {{- toYaml .stringData | nindent 2 }}
{{- end }}    
