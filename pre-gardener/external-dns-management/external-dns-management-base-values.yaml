apiVersion: v1
kind: ConfigMap
metadata:
  name: external-dns-management-base-values
  namespace: flux-system
data:
  values.yaml: |-
    external-dns-management:
      enabled: true
      configuration:
        dnsClass: base-dns-class
        identifier: ${CLUSTERIDENTITY:=garden-cluster-identity}-base-dns
